%section.cover-img-section
  .cover-img
    #search-col.col-md-8
      #search-panel.panel.panel-default
        .panel-body
          %h1 Get enlightened
          .btn-group(data-toggle="buttons")
            %label.btn.btn-default.active
              %input#by-month(autocomplete="off" name="options" type="radio" checked="checked")
              Filtered by month
            %label#by-location.btn.btn-default
              %input(autocomplete="off" name="options" type="radio")
              Filtered by location
            %label#by-language.btn.btn-default
              %input(autocomplete="off" name="options" type="radio")
              Filtered by language

%h1.root-page-title.text-center Events for you

%section#event-list-section
  = render partial: "event_list"
